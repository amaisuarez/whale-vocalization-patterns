[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "whalecodas"
version = "0.1.0"
description = "Unsupervised analysis of sperm whale coda structure"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
